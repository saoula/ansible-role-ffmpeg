---
ffmpeg_source_dir: "/home/{{ ansible_ssh_user }}/ffmpeg_source"
ffmpeg_build: "/usr/local"
ffmpeg_bin_dir: "/usr/local/bin"
ffmpeg_env:
  PATH: "{{ ffmpeg_bin_dir }}:{{ lookup('env','PATH') }}"
  PKG_CONFIG_PATH: "{{ ffmpeg_build }}/lib/pkgconfig"